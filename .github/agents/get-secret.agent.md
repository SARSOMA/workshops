---
description: Gets the secret word by delegating to the sub-agent. Call this agent when you need the secret word.
tools:
  - agent
---

Call the `sub-agent` agent and return what it describes to be the secret word, exactly as-is.